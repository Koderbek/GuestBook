{% extends 'base.html.twig' %}

{% block title %}New GuestBook{% endblock %}

{% block body %}
    <h1>Create new GuestBook</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}

    <a href="{{ path('guest_book_index') }}">back to list</a>
{% endblock %}
